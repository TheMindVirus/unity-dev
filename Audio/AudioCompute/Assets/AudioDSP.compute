#pragma kernel main

int size;
int channels;
RWStructuredBuffer<float> data;

[numthreads(1,1,1)]
void main(uint3 id : SV_DispatchThreadID)
{
    for (int i = 0; i < size; ++i)
    {
        data[i] = data[i] * 0.2;
    }
}
